
// these are secret variables that are stuffed into scope at certain points.
// They have names that can't be used because they have illegal chars in them
export const fileSourceDir = '(file source directory)';
export const shellFlag = '(shellFlag)';
